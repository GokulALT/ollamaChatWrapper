{
  "name": "mcp-server-example",
  "version": "1.0.0",
  "description": "A standalone MCP server that connects to Ollama and provides filesystem and echo tools.",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@model-context-protocol/server": "^0.0.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
