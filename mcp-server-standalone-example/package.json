{
  "name": "mcp-server-standalone-example",
  "version": "1.0.0",
  "description": "A standalone, all-in-one MCP server, written in TypeScript, that connects to Ollama and exposes an echo tool.",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@model-context-protocol/server": "^0.0.8",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
